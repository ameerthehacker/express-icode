config = {
    database: {
        name: 'localhost/icode',
        user: '',
        password: ''
    },
    application: {
        secret: 'secret',
        env: 'development'
    }
}

module.exports = config;